version: '3.8'

services:
  jupyterlab:
    build: .
    image: dynamo-workshop:latest
    container_name: dynamo-workshop-jupyterlab
    ports:
      - "8888:8888"
    volumes:
      - ./:/workspace
    working_dir: /workspace
    stdin_open: true
    tty: true

